import 'package:flutter/material.dart';

class ErrorWidgetsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('error widgets'),
    );
  }
}
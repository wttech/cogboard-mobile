import 'package:flutter/material.dart';

class FavoriteWidgetsScreen extends StatelessWidget {
  //TODO #9
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('favourite widgets'),
    );
  }
}

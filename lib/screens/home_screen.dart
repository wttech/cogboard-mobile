import 'package:flutter/material.dart';

class HomeWidgetsScreen extends StatelessWidget {
  // TODO: #6, #5, #15
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('home widgets'),
    );
  }
}
import 'package:flutter/material.dart';

class WarningWidgetsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('warning widgets'),
    );
  }
}